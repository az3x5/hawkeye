.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.env

# Model weights: fetched per environment, verified by checksum, never committed.
models/

# Operational data drops. CSV/people.csv holds real identity records including
# national ID numbers; it is ignored so a routine `git add -A` cannot commit it.
CSV/
# SQLAlchemy leaves this behind when a session is pointed at an in-memory URL.
backend/:memory:*
